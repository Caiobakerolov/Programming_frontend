const multiplication = (area: number, height: number) => area * height;
console.log(multiplication(31, 4));


function salutation(nome: string): string {
  return `Hello ${nome}`;
}

console.log(salutation("Caio"));
